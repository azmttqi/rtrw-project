import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors based on Logo
  static const Color primaryGreen = Color(0xFF678267); // Requested field button color
  static const Color primaryYellow = Color(0xFFEBB54A); // Yellow from the mock 'Tambah kegiatan' button

  // Neutral Colors
  static const Color backgroundLight = Color(0xFFF8F2E5); // Requested background color
  static const Color backgroundDark = Color(0xFF121212); // Dark mode background
  
  static const Color surfaceLight = Color(0xFFFFFFFF); // White cards/surfaces
  static const Color surfaceDark = Color(0xFF1E1E1E); // Dark mode surface

  static const Color textPrimaryLight = Color(0xFF212529); // Dark gray for text
  static const Color textSecondaryLight = Color(0xFF6C757D); // Medium gray for secondary text
  
  static const Color textPrimaryDark = Color(0xFFF8F9FA);
  static const Color textSecondaryDark = Color(0xFFADB5BD);

  // Status Colors
  static const Color error = Color(0xFFDC3545);
  static const Color success = Color(0xFF28A745);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF17A2B8);
}
